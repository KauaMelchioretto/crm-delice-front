import { Box } from "@mui/joy"

export const Customers = () => {
    return (
        <Box
        sx={{
            height: "100%",
            width: "100%",
            gap: 2,
            display: "flex",
            flexDirection: "column"
        }}
        ></Box>
    );
}